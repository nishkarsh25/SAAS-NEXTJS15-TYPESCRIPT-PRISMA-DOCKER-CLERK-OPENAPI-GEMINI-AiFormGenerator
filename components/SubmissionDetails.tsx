import React from "react";
import {
  Table,
  TableBody,
  TableCell,
  TableHead,
  TableHeader,
  TableRow,
} from "./ui/table";

type Props = {
  submission: any;
  index: number;
};

const SubmissionsDetails: React.FC<Props> = ({ submission, index }) => {
 
  return (
    <div className="mt-8">
      
    </div>
  );
};

